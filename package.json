{
  "name": "address-suggest",
  "version": "1.0.0",
  "private": false,
  "description": "Address suggest widget with Yandex APIs",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "0.23.0"
  }
}
